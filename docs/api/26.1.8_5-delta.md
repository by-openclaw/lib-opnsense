# OPNsense 26.1.5 → 26.1.8_5 — API Delta Report

> Generated: 2026-05-21 from `probe-api-schemas.sh` runs against `vm-opns-01.by-research.be` (26.1.8_5)
> Baseline: previous probe data at `docs/api/data/26.1.5/`
> Current: `docs/api/data/26.1.8_5/`

## Summary

| Metric | Value |
|---|---|
| Total endpoints probed | 200 (identical to 26.1.5) |
| HTTP 200 (working) | 196 |
| HTTP 404 (uninstalled plugins, not deprecated) | 4 — `chrony`, `lldpd` (os-* plugins) |
| Schema files with diff | 23 of 66 |
| New endpoint URLs | 0 |
| Removed endpoint URLs | 0 |

**Conclusion:** No new MVC controllers in 26.1.6/.7/.8 — all changes are **field-level additions inside existing endpoints** (matches release notes: Kea DDNS, captive portal IPv6, Unbound alias rename).

## Field-level changes — per manager work list

### Kea DHCP (biggest set of additions)

`kea4-subnet` + `kea6-subnet` — 8 new fields each, all DDNS-related:

- `ddns_conflict_resolution_mode`
- `ddns_dns_port`
- `ddns_override_client_update`
- `ddns_override_no_update`
- `ddns_qualifying_suffix`
- `ddns_reverse_zone`
- `ddns_update_on_renew`
- `valid_lifetime`

`kea4-reservation`: `+client_id`
`kea6-reservation`: `+prefix`

**Lib work:** extend `Kea4SubnetManager`, `Kea6SubnetManager`, `Kea4ReservationManager`, `Kea6ReservationManager` to support new fields. Update `models/dhcp/*.py` dataclasses.

### Other field additions

| Manager / endpoint | Change |
|---|---|
| `cp-zone` (captive portal) | `+ roaming` (26.1.6 IPv6 support) |
| `routing-gw` | `+ nosync` (HA failover) |
| `ub-host-override` (Unbound) | `+ aliases` (replaces `aliascount`) |
| `ovpn-instance` | +23 bytes — minor field added |
| `trust-ca`, `trust-cert` | +11 bytes — minor field added |
| `if-lagg` | +243 bytes — likely new options |
| `if-vlan` | +122 bytes — likely new options |
| `ipsec-child`, `ipsec-conn` | +543 bytes each — multiple fields |
| `cron-job` | +68 bytes |

### Field reductions (potentially deprecated)

| Manager | Change |
|---|---|
| `auth-group`, `auth-user` | -130 bytes each — minor field removed |
| `fw-alias` | -219 bytes — possibly a field gone |
| `fw-filter-rule` | -84 bytes |
| `ipsec-remote` | -28 bytes |

### Value-only changes (no key add/remove)

| Manager |
|---|
| `ipsec-local`, `monit-service`, `syslog-dest` |

Likely enum value additions or default changes — needs deep diff.

## Plugin endpoints

Both **os-chrony** and **os-lldpd** are now installed on vm-opns-01 and have **full MVC API**.

| Plugin | Endpoints (HTTP 200) | Manager to add in lib |
|---|---|---|
| `os-chrony` | `general/get`, `general/set`, `service/status`, `reconfigure`, `restart`, `start`, `stop` | **`ChronyGeneralManager`** (new) |
| `os-lldpd` | `general/get`, `general/set`, `service/status`, `reconfigure`, `restart`, `start`, `stop`, `service/neighbor` | **`LldpdGeneralManager`** + read-only neighbor query (new) |

These were marked ABSENT in the previous coverage table because the system probed at 26.1.5 didn't have them installed. They are NOT deprecated — they're optional plugins with proper MVC API.

## Action items

| # | Item | Effort |
|---|------|--------|
| 1 | Update `KeaSubnetManager` (v4 + v6) + models to support 8 new DDNS fields | S |
| 2 | Update `KeaReservationManager` v4 (client_id) + v6 (prefix) | XS |
| 3 | `CpZoneManager`: add `roaming` field | XS |
| 4 | `RtGatewayManager`: add `nosync` field | XS |
| 5 | `UbHostOverrideManager`: rename `aliascount` → `aliases` (backward-compat alias) | S |
| 6 | Deep field diff for `ipsec-child`, `ipsec-conn`, `if-lagg`, `if-vlan` | S |
| 7 | Investigate field removals in `auth-*`, `fw-alias`, `fw-filter-rule` | M (verify not actually deprecated) |
| 8 | Update `verify-validators.py` against 26.1.8_5 source | S |
| 9 | Update `docs/api-coverage.md` to mark baseline as 26.1.8_5 | XS |
| 10 | Bump lib to v1.2.0 with `> Probed against OPNsense **26.1.8_5**` header | XS |

**One PR per scope** per [no_multi_repo_pr_batch](file:///home/by-systems/.claude/projects/-home-by-systems/memory/feedback_no_multi_repo_pr_batch.md): Kea PR, captive portal PR, routing PR, DNS PR, etc. Estimated 5-7 PRs.
